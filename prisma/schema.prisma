generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Lead {
  id        Int      @id @default(autoincrement())
  fullName  String
  email     String
  phone     String
  program   String
  createdAt DateTime @default(now())
}
